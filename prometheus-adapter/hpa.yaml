apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: txtextractor-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: txtextractor
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: External
    external:
      metric:
        name: queue-length
        selector: {
          matchLabels: {
            channel: "s1-request",
            type: "queues"
          }
        }
      target:
        type: Value
        value: 0.05
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 30 # 30 secs
      policies:
      - type: Pods
        value: 2
        periodSeconds: 30 # wait 30 seconds before scaling down (2 by 2)
      selectPolicy: Min